import { getDataAPI, postDataAPI, putDataAPI, deleteDataAPI } from '../../api/api';
import * as ACTIONS from "../constants/socketContant";


export const addPost = (data, token) => async (dispatch) => {
    try {
       
    } catch (err) {
        console.log(err);
    }
}
